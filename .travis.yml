# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
matrix:
  include:
  - r: devel
  - r: release
  - r: oldrelease
  - r: release
    os: osx
    osx_image: xcode7.3

cache: packages
